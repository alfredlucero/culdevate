# DO NOT EDIT THIS FILE. THIS IS USED FOR EXAMPLE PURPOSES
# When running now dev locally, we'll need to define the environment variables 
# used through process.env.* at runtime
# When running in production, it will consult the now.json's secrets set with the env variables
# Copy this file, set the proper environment variable values, and rename to .env
# before starting up now dev
MONGODB_URI=mongodb+srv://<user>:<password>@<mongodb_path>/test?retryWrites=true&w=majority

