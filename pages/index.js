import React from "react";
import styled from "styled-components";

const CuldevateApp = () => (
  <div>
    <Title>Culdevate App</Title>
  </div>
);

const Title = styled.h1`
  color: cyan;
`;

export default CuldevateApp;
